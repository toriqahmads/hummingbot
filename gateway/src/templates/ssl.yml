# need more details...
caCertificatePath: /usr/src/app/certs/ca_cert.pem

# need more details...
certificatePath: /usr/src/app/certs/server_cert.pem

# need more details...
keyPath: /usr/src/app/certs/server_key.pem

# File where the gateway passphrase is stored. This passphrase is used to sign
# https responses and encrypt/decrypt private keys stored on gateway. The file
# must use 0600 permissions for security purposes.
passPhrasePath: /usr/src/app/conf/gateway-passphrase.yml
